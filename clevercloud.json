{
  "type": "php",
  "web": {
    "document_root": "public/"
  },
  "deploy": {
    "preinstall": "composer install --no-dev --optimize-autoloader",
    "postinstall": "php artisan config:cache && php artisan route:cache && php artisan view:cache"
  }
}
